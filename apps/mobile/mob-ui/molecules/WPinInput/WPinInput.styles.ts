import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	input: {
		width: "100%",
		textAlign: "center",
		textAlignVertical: "center",
	},
	inputRow: { width: 56, paddingHorizontal: 0 },
});
