import { Canvas, Meta, Controls } from '@storybook/blocks';

import * as GalleryStories from './MGallery.stories';

<Meta of={GalleryStories} />

# Definition

Gallery is used to display any elements as slidable items.

<Canvas of={GalleryStories.Basic} />

<Controls />
